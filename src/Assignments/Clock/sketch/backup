background("#C2E2FA");
  noStroke();
  circle(mouseX, mouseY, 25);

  for (let x = 0; x < width; x += 50) {
    stroke("white");
    line(x, 0, x, height - 50);
  }
  for (let y = 0; y < height; y += 50) {
    line(0, y, width - 50, y);
  }

  push();
  fill("#A1BC98");
  noStroke();
  ellipse(width / 2, height - 50, width * 1.2, height - 250);
  pop();

  push();
  cx = width / 2;
  cy = height / 2;

  //앞다리 뒤쪽
  push();
  stroke("#E3E3E3");
  strokeWeight(30);
  line(cx + 50, cy, cx + 50, cy + 100);

  //뒷다리 뒤쪽
  line(cx - 70, cy, cx - 70, cy + 100);
  circle(cx - 75, cy + 20, 45);
  pop();

  // 몸
  fill("white");
  noStroke();
  ellipse(cx, cy, 200, 100);

  // 머리
  circle(cx + 100, cy - 20, 100);

  // 귀
  triangle(cx + 50, cy - 100, cx + 50, cy - 20, cx + 150, cy - 20);
  triangle(cx + 50 * 3, cy - 100, cx + 50, cy - 20, cx + 150, cy - 20);

  // 뒷다리
  stroke("white");
  strokeWeight(30);
  line(cx - 90, cy, cx - 90, cy + 100);
  circle(cx - 75, cy + 20, 45);

  // 앞다리
  line(cx + 70, cy, cx + 70, cy + 100);

  //꼬리
  strokeWeight(20);
  line(cx - 90, cy - 20, cx - 200, cy - 100);
  pop();